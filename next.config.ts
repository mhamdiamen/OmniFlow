import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: [
      "example.com",
      "lh3.googleusercontent.com",
      "github.com",
      "benevolent-reindeer-120.convex.cloud", // Add this line
      "fiery-dalmatian-147.convex.cloud",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
